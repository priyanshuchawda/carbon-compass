import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ReportPage() {
  return (
    <PagePlaceholder
      eyebrow="Step 5"
      title="Carbon Compass report"
      description="Generate a clean summary with monthly footprint, top source, best action, potential saving, and transparent assumptions."
    />
  );
}
